import 'package:flutter_test/flutter_test.dart';

import 'package:dualswitch/dualswitch.dart';

void main() {
  test('adds one to input values', () {
    final calculator = DualSwitch(onChanged: (value) {
      
    },);
  });
}
